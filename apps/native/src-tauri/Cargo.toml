[package]
name = "personal-native"
version = "0.1.0"
description = "Native shell for Luka's personal platform"
edition = "2021"
rust-version = "1.85"

[lib]
name = "personal_native_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.3", features = [] }
